# Original benchmark evidence

These are the 75 original per-target files from successful analysis in Argo workflow
`speed-comparison-manual-xj2px`, recovered from its archived `benchmark-results`
bundle. `SHA256SUMS` records the original file bytes. The existing combined report,
chart, source revision and publication date have not been changed.

The source is `c71e4cd81464b238172605982806c5bf827a70a1`. The workload is one billion
rounds. This original protocol had two warmups, three measured samples and a separate
output execution. Each JSON retains its individual samples, exit codes, calculated
pi, commands, methodology and resolved environment. Missing fields remain missing;
the later five-execution protocol must not be inferred for this historical run.

Original artifact key:
`argo-workflows/speed-comparison/speed-comparison-manual-xj2px/speed-comparison-manual-xj2px-analyze-3360498425/benchmark-results.tgz`

Preserving the raw files here keeps them available independently of Argo's log and
artifact retention. Publication recovery was `speed-comparison-republish-lkllt`.
